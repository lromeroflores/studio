apiVersion: v1
kind: Service
metadata:
  name: {{ include "contract-ease.fullname" . }}
  labels:
    {{- include "contract-ease.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "contract-ease.selectorLabels" . | nindent 4 }}
