apiVersion: v1
kind: Service
metadata:
  name: {{ include "contract-ease-app.fullname" . }}
  labels:
    {{- include "contract-ease-app.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "contract-ease-app.selectorLabels" . | nindent 4 }}
